---
description: Show the sharedserver config (servers and profiles)
allowed-tools: Bash(sharedserver:*)
---
Show the user their sharedserver config (servers and profiles), verbatim:

!`sharedserver config show`
