---
description: Show sharedserver-managed backend processes (running servers, PIDs, refcounts)
allowed-tools: Bash(sharedserver:*)
---
Report the current sharedserver state to the user, as-is:

!`sharedserver list`
